apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: default
data:
  APP_ENV: "production"
  SERVICE_NAME: "user-service"
  DATABASE_URL: "postgresql://$DATABASE_USER:$DATABASE_PASSWORD@parking-automation-postgres-rw.parking-automation.svc.cluster.local:5432/user-service"
  REDIS_URL: "redis://:$REDIS_PASSWORD@parking-automation-redis-master.parking-automation.svc.cluster.local:6379/0"
